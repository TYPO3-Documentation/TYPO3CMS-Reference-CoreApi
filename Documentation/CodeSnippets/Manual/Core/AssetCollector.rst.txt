..  Generated by https://github.com/TYPO3-Documentation/t3docs-codesnippets
..  php:namespace::  TYPO3\CMS\Core\Page

..  php:class:: AssetCollector

    The Asset Collector is responsible for keeping track of
    - everything within <script> tags: javascript files and inline javascript code
    - inline CSS and CSS files

    The goal of the asset collector is to:
    - utilize a single "runtime-based" store for adding assets of certain kinds that are added to the output
    - allow to deal with assets from non-cacheable plugins and cacheable content in the Frontend
    - reduce the "power" and flexibility (I'd say it's a burden) of the "god class" PageRenderer.
    - reduce the burden of storing everything in PageRenderer

    As a side effect this allows to:
    - Add a single CSS snippet or CSS file per content block, but assure that the CSS is only added once to the output.

    Note on the implementation:
    - We use a Singleton to make use of the AssetCollector throughout Frontend process (similar to PageRenderer).
    - Although this is not optimal, I don't see any other way to do so in the current code.

    ..  php:method:: addJavaScript(string $identifier, string $source, array $attributes = [], array $options = [])
        :returns: `self`

        :param $identifier: the identifier
        :param $source: URI to JavaScript file (allows EXT: syntax)
        :param $attributes: additional HTML <script> tag attributes, default: []
        :param $options: ['priority' => true] means rendering before other tags, default: []

    ..  php:method:: addJavaScriptModule(string $identifier)
        :returns: `self`

        :param $identifier: Bare module identifier like @my/package/Filename.js

    ..  php:method:: addInlineJavaScript(string $identifier, string $source, array $attributes = [], array $options = [])
        :returns: `self`

        :param $identifier: the identifier
        :param $source: JavaScript code
        :param $attributes: additional HTML <script> tag attributes, default: []
        :param $options: ['priority' => true] means rendering before other tags, default: []

    ..  php:method:: addStyleSheet(string $identifier, string $source, array $attributes = [], array $options = [])
        :returns: `self`

        :param $identifier: the identifier
        :param $source: URI to stylesheet file (allows EXT: syntax)
        :param $attributes: additional HTML <link> tag attributes, default: []
        :param $options: ['priority' => true] means rendering before other tags, default: []

    ..  php:method:: addInlineStyleSheet(string $identifier, string $source, array $attributes = [], array $options = [])
        :returns: `self`

        :param $identifier: the identifier
        :param $source: stylesheet code
        :param $attributes: additional HTML <link> tag attributes, default: []
        :param $options: ['priority' => true] means rendering before other tags, default: []

    ..  php:method:: addMedia(string $fileName, array $additionalInformation)
        :returns: `self`

        :param $fileName: the fileName
        :param $additionalInformation: One dimensional hash map (array with non-numerical keys) with scalar values

    ..  php:method:: removeJavaScript(string $identifier)
        :returns: `self`

        :param $identifier: the identifier

    ..  php:method:: removeInlineJavaScript(string $identifier)
        :returns: `self`

        :param $identifier: the identifier

    ..  php:method:: removeStyleSheet(string $identifier)
        :returns: `self`

        :param $identifier: the identifier

    ..  php:method:: removeInlineStyleSheet(string $identifier)
        :returns: `self`

        :param $identifier: the identifier

    ..  php:method:: removeMedia(string $identifier)
        :returns: `self`

        :param $identifier: the identifier

    ..  php:method:: getMedia()
        :returns: `array`

    ..  php:method:: getJavaScripts(?bool $priority = NULL)
        :returns: `array`

        :param $priority: the priority, default: NULL

    ..  php:method:: getInlineJavaScripts(?bool $priority = NULL)
        :returns: `array`

        :param $priority: the priority, default: NULL

    ..  php:method:: getJavaScriptModules()
        :returns: `array`

    ..  php:method:: getStyleSheets(?bool $priority = NULL)
        :returns: `array`

        :param $priority: the priority, default: NULL

    ..  php:method:: getInlineStyleSheets(?bool $priority = NULL)
        :returns: `array`

        :param $priority: the priority, default: NULL

    ..  php:method:: hasJavaScript(string $identifier)
        :returns: `bool`

        :param $identifier: the identifier

    ..  php:method:: hasInlineJavaScript(string $identifier)
        :returns: `bool`

        :param $identifier: the identifier

    ..  php:method:: hasStyleSheet(string $identifier)
        :returns: `bool`

        :param $identifier: the identifier

    ..  php:method:: hasInlineStyleSheet(string $identifier)
        :returns: `bool`

        :param $identifier: the identifier

    ..  php:method:: hasMedia(string $fileName)
        :returns: `bool`

        :param $fileName: the fileName
