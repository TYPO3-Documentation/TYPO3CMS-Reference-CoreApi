services:
  # ... other configuration

  myRateLimiter:
    class: TYPO3\CMS\Core\RateLimiter\RateLimiterFactory
    arguments:
      $config:
        id: 'my-custom-limiter'
        policy: 'sliding_window'
        limit: 5
        interval: '10 minutes'
