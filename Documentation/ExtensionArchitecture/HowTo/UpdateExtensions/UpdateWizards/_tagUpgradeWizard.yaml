services:
  # Place here the default dependency injection configuration

  # Only necessary when "autoconfigure" is set to false!
  MyVendor\MyExtension\Upgrades\ExampleUpgradeWizard:
    tags:
      - name: install.upgradewizard
